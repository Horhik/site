html{
    background: black;
    color: white;
}
body {
    font-family: "mononoki";
}

header {
    margin: 0 auto;
}

*content{
    width: 80vw;
}

header ul {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    flex-grow: 1;
    font-size: 2em;
}

header li {
    list-style: none;
    padding: 10px;
}

a {
    color: #888;
}

a:visited {
    text-decoration: #555;
}

a:hover, a:focus {
  background: lightcoral;
  color: black;
}

a:active{
  background: coral;
  color: black;
  text-decoration: none;
}
